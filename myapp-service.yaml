apiVersion: v1
kind: Service
metadata:
  name: app
spec:
  type: NodePort
  selector:
    app: app
  ports:
  - port: 80
    targetPort: 5050
    #nodePort: 31529   # optional, or let Kubernetes assign
